import basicLogPrefix from "./basicLogPrefix.mjs";
import formatMessage from "./formatMessage.mjs";
class BasicLayout {
  /**
   * Format the log event (格式化日志事件)
   * @param event Log event (日志事件)
   */
  format(event) {
    return `${basicLogPrefix(event)} ${formatMessage(event.message)}`;
  }
}
export {
  BasicLayout as default
};
