"use strict";
/*! date-manip v2.0.4 | MIT License */
const isWhatType = require("is-what-type");
const addObject = require("./addObject-D9QjdlJK.js");
function subtract(date, num, unit) {
  return isWhatType.isObject(num) ? addObject.addObject(date, num, -1) : addObject.addNumber(date, -num, unit);
}
module.exports = subtract;
