/*! date-manip v2.0.4 | MIT License */
import { a as absFloor } from "./absFloor-Q8SXASBH.mjs";
import { b as MS_IN_MINUTE } from "./constants-D-k1kkaO.mjs";
import parse from "./parse.mjs";
function diffInMinutes(date, input, asFloat) {
  const parsed = parse(input);
  const output = (+date - +parsed) / MS_IN_MINUTE;
  return asFloat ? output : absFloor(output);
}
export {
  diffInMinutes as default
};
