"use strict";
/*! date-manip v2.0.4 | MIT License */
const absFloor = require("./absFloor-CekhEiWG.js");
const constants = require("./constants-4rlwwz6o.js");
const parse = require("./parse.js");
function diffInSeconds(date, input, asFloat) {
  const parsed = parse(input);
  const output = (+date - +parsed) / constants.MS_IN_SECOND;
  return asFloat ? output : absFloor.absFloor(output);
}
module.exports = diffInSeconds;
